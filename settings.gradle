pluginManagement {
    repositories {
        maven {
            url = "https://packages.confluent.io/maven/"
        }
        maven {
            url = "https://jitpack.io"
        }
        gradlePluginPortal() 
    }
}

rootProject.name = 'kafka-playground'

